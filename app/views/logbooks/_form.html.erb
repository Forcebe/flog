<%= form_for @logbook do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

   <%# TODO: Decide types, make a select box. restrict in the model %>
  <%= f.label :aviation_type %>
  <%= f.select:aviation_type, options_for_select(['General Aviation', 'Gliding', 'Free Flight', 'Skydiving'], @logbook.aviation_type) %>

  <%= f.submit %>
<% end  %>
